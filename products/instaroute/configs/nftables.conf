#!/usr/sbin/nft -f
#
# Auto-generated by InstaGuard Engine v0.1.0
# Generated at: 2026-04-18T17:05:20+05:30
# DO NOT EDIT — changes will be overwritten on next apply.
#

flush ruleset

table inet firewall {
    chain input {
        type filter hook input priority 0;
        policy drop;

        ct state established,related accept
        iif "lo" accept

        # Management access (always allowed)
        tcp dport 22 accept

        # DNS from LAN clients
        udp dport 53 iifname "enp2s0" accept
        tcp dport 53 iifname "enp2s0" accept

        # DHCP from LAN clients
        udp dport 67 iifname "enp2s0" accept

        # InstaGuard Engine API (LAN only)
        tcp dport 8080 iifname "enp2s0" accept

        # ICMP
        ip protocol icmp accept
    }

    chain forward {
        type filter hook forward priority 0;
        policy drop;

        ct state established,related accept

        # [2] block-telnet — Block insecure telnet outbound
        iifname "enp2s0" oifname "enp1s0" tcp dport 23 drop

        # [1] allow-lan-to-wan — Allow LAN users to reach the internet
        iifname "enp2s0" oifname "enp1s0" accept
    }

    chain output {
        type filter hook output priority 0;
        policy accept;
    }
}

table ip nat {
    chain postrouting {
        type nat hook postrouting priority 100;
        oif "enp1s0" masquerade
    }
}
